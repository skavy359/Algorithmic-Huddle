// User function Template for C

void utility() {

    // declare the variable count here
    static int count=0;
    
    // increases the value of count by 1
    count++;

    // print count
    printf("%d", count);
}
