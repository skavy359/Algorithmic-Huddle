void utility(bool a, bool b)
{
    // Do a AND b below
    bool p = a && b;
    // Do a OR b below
    bool q = a || b;
    // Do NOT a below
    bool r = !a;

    cout << boolalpha << p << " " << q << " " << r << endl;
}