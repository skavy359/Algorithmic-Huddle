void arrayTraversalReverse(int arr[], int size)
{
    // Write your code below
    for (int i = size - 1; i >= 0; i--)
    {
        cout << arr[i] << " ";
    }

    // Don't add a new line as it is provided by the driver code
}